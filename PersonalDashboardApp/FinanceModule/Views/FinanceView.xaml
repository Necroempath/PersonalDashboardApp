<UserControl x:Class="PersonalDashboardApp.FinanceModule.Views.FinanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PersonalDashboardApp.FinanceModule.Views"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Finance Record Editor" FontSize="26" FontWeight="Bold" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="10,0,0,10"/>
            
            <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Margin="10" VerticalAlignment="Center"  Grid.ColumnSpan="2">
                <TextBlock Text="Income"  FontSize="16"/>
                <RadioButton IsChecked="{Binding Type, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Income}" GroupName="TransactionType" Margin="5,5,0,0"/>
                <TextBlock Text="Expense"  FontSize="16" Margin="60,0,0,0"/>
                <RadioButton IsChecked="{Binding Type, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Expense}" GroupName="TransactionType" Margin="5,5,0,0"/>
            </StackPanel>
            
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Category:" FontSize="16" Margin="10"/>
            <ComboBox SelectedItem="{Binding Category.Name, Mode=TwoWay}" DisplayMemberPath="Name" x:Name="CategoryComboBox" Grid.Row="2" Grid.Column="1" Height="25" Width="200" HorizontalAlignment="Left" FontSize="14"/>
            
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Amount:" FontSize="16" Margin="10"/>
            <TextBox Text="{Binding Amount}" x:Name="AmountTextBox" Grid.Row="3" Grid.Column="1" Height="25" Width="200" HorizontalAlignment="Left" FontSize="14"/>
            
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Transaction date:" FontSize="16" Margin="10"/>
            <DatePicker SelectedDate="{Binding Date}" x:Name="TransactionTimeDatePicker" Grid.Row="4" Grid.Column="1" Height="25" Width="200" HorizontalAlignment="Left" FontSize="14"/>
            
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Note:" FontSize="16" Margin="10"/>
            <TextBox Text="{Binding Note}" x:Name="Note" Grid.Row="5" Grid.Column="1" Height="25" Width="200" HorizontalAlignment="Left" FontSize="10"/>

            <Button Grid.Row="6" Grid.Column="0" Content="Add" FontSize="16" VerticalAlignment="Bottom" Width="100" Margin="5" Click="ButtonBase_OnClick"/>
            <Button x:Name="UpdateButton" Grid.Row="7" Grid.Column="0" Content="Update" FontSize="16" Width="100" IsEnabled="False" Margin="5" Click="UpdateButton_OnClick"/>
            <Button x:Name="DeleteButton" Grid.Row="8" Grid.Column="0" Content="Delete" FontSize="16" Width="100" IsEnabled="False" Margin="5" Click="DeleteButton_OnClick"/>
        </Grid>
        
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Finance Records Viewer" FontSize="26" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,10"/>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox Width="300" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/> 
                <Button Content="Search" Width="80" Height="20" FontSize="14"/>
            </StackPanel>

            <DataGrid x:Name="FinanceDataGrid"
                      Grid.Row="2"
                      SelectionMode="Single"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="False"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ItemsSource="{Binding FinanceRecords}">
                    <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Title}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Deadline}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Priority}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Transaction date" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Note" Binding="{Binding Status}" Width="100" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>

</UserControl>
